# Memory — 2026-03-31

## Scheduler Changes
- Scheduler now fires at **5:00am PDT** (changed from 8:30am) so Nicole (EDT) can run at 8am her time
- LaunchAgent plist updated and reloaded: `/Users/darrenduffy/Library/LaunchAgents/ai.getnarrow.larry.plist`
- Lock file added to scheduler (`logs/scheduler.lock`) — prevents two scheduler instances running simultaneously (was causing parallel sessions on March 27)
- Lock auto-released on exit/SIGINT/SIGTERM; stale after 6 hours

## Bugs Fixed (this session)

### Post Engagement — LinkedIn Selector Overhaul
- LinkedIn switched from Quill (`.ql-editor`) to **TipTap/ProseMirror** editor
- New comment editor selector: `[aria-label="Text editor for creating comment"]`
- New submit button: `button:has-text("Submit")` scoped to **page level** (not post element — may render outside listitem)
- Submit button polling: up to 8x / 300ms to wait for enabled state
- `page.keyboard.type()` required instead of `element.type()` — TipTap needs page-level keyboard events to update internal state
- Like selector updated: `button[aria-label*="Reaction button state: no reaction"]` (was `aria-pressed="false"` which no longer works)
- Post containers: `div[role="listitem"]` (no `data-id` attributes anymore)

### Onboarding Bugs (all new accounts affected)
1. **Offer description always blank** — `config-loader.js` read `section('OFFER & CTA')` but generated ACCOUNT.md has `## OFFER & VALUE PROP` + `## CTA & MESSAGING`. Fixed to read both and combine.
2. **HISTORY.md never recorded** — `logToHistory()` searches for `## Log` header but webhook generated table-only format with no such header. Fixed webhook to include `## Log\n` in generated HISTORY.md.
3. **hasRunToday() always false** — because #2 was broken, `### YYYY-MM-DD` entries were never written, profiles could double-run. Fixed via #2.
- Nicole's existing HISTORY.md patched manually to add `## Log` header.

### Follow-up Messages
- Name extraction fixed: strip `"View "` prefix and `"'s profile"` suffix from `aria-label`
- Message button: changed from exact `a[aria-label="Message"]` to `[aria-label*="Message"]` (contains match — LinkedIn includes person's name in aria-label)

## Current Profile Status (as of 2026-03-31 5am run)
| Profile | Connections | Likes | Comments | InMails | Notes |
|---------|-------------|-------|----------|---------|-------|
| Chris | 0 | 7 | 0 | 2 (duplicate to same person) | Search exhausted |
| Darren | 35 | 2 | 0 | 2 ✅ | Search active |
| Nicole | 35 | — | — | 0 (15 checked) | Search active |

## Outstanding Issues
- **Comments still 0** — TipTap fix deployed, not yet confirmed working (tomorrow's run will verify)
- **Chris Sales Nav search exhausted** — needs new saved search (same as Darren was)
- **InMail duplicate** — Chris sent same InMail to Michael Kanaby twice; no "already messaged" dedup guard in inmails.js
- **Darren Sales Nav** — was exhausted, appears active again today (35 sent)

## Key Files Changed
- `scripts/scheduler.js` — lock file logic added
- `scripts/phases/post-engagement.js` — full selector overhaul for new LinkedIn feed
- `scripts/phases/follow-ups.js` — name extraction + Message button selector fix
- `scripts/utils/config-loader.js` — offer section name fix (OFFER & VALUE PROP + CTA & MESSAGING)
- `scripts/webhook-server.js` — HISTORY.md `## Log` header fix for new profiles
- `profiles/nicolepindul/HISTORY.md` — patched manually
- `/Users/darrenduffy/Library/LaunchAgents/ai.getnarrow.larry.plist` — 5am start time
